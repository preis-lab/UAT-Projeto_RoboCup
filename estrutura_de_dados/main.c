#include <stdlib.h>
#include <stdlib.h>
#include <string.h>

#include "pilha_INSCRICAO.h"

int main(){
    tipopilha lancamento;

    lancamento = malloc(sizeof(struct no));

    INIT(&lancamento);
    PUSH()

}
